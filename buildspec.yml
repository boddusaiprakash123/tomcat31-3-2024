version: 0.2
phases:
  install:
    runtime-versions:
      java: corretto8
  build:
    commands:
      - mvn clean package
      - aws s3 cp target/myapp.war s3://tomcat1
artifacts:
  files:
    - myapp.war
